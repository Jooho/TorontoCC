apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: auth
  namespace: chat
spec:
  hosts:
  - '*'
  http:
  - route:
    - destination:
        host: auth
        port:
          number: 8080
    timeout: 1s

