apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: chat-server
  namespace: chat
spec:
  gateways:
  - chat-gateway
  hosts:
  - chat-server-example-chat.apps.toronto.openshiftworkshop.com
  http:
  - match:
    - uri:
        prefix: /
    route:
    - destination:
        host: chat-server
